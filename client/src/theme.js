import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#757575",
    },
    secondary: {
      main: "#cfd8dc",
    },
  },
});

export default theme;
