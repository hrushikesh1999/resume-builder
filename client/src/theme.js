import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#757575",
    },
    secondary: {
      main: "#424242",
    },
  },
});

export default theme;
